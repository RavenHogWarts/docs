# Known Issues

## Conflict between `Frontmatter Sorter` automatic sorting and `Update time on edit` plugin
Currently, you must choose one or the other: either disable automatic sorting or disable the `Update time on edit` plugin

To work around this when automatic sorting is disabled, you can set a keyboard shortcut (e.g., `Alt + s`) for the `Sort Current File` command to enable convenient manual sorting
